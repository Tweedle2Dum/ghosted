import { SignupPage } from "@/views/signup-page";

export const metadata = {
  title: "Create Account | Ghosted Workspace",
  description: "Start deploying autonomous services with Ghosted today.",
};

export default function Page() {
  return <SignupPage />;
}
