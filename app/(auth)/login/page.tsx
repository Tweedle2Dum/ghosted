import { LoginPage } from "@/views/login-page";

export const metadata = {
  title: "Sign In | Ghosted Workspace",
  description: "Authenticate to access your Ghosted autonomous workspace.",
};

export default function Page() {
  return <LoginPage />;
}
