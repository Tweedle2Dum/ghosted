import { ProjectsPage } from "@/views/projects-page";

export const metadata = {
  title: "Projects | Ghosted Workspace",
  description:
    "Browse, filter, and inspect autonomous repositories and submodules.",
};

export default function Page() {
  return <ProjectsPage />;
}
